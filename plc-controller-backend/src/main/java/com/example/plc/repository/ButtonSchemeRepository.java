package com.example.plc.repository;

import com.example.plc.entity.ButtonScheme;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ButtonSchemeRepository extends JpaRepository<ButtonScheme, Long> {
}
